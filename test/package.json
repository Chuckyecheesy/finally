{
  "name": "finally-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end tests for the FinAlly trading workstation (PLAN.md §12).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
