---
phase: 03-performance-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useTerminal.ts
  - frontend/src/hooks/useTerminal.test.ts
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Backgrounding the browser tab (document.visibilityState === 'hidden') stops the 15s REST reconciliation poll started by useTerminal"
    - "Foregrounding the tab again resumes the poll, including an immediate refresh"
    - "A visible tab still polls every 15s exactly as before this change"
  artifacts:
    - path: "frontend/src/hooks/useTerminal.ts"
      provides: "visibility-gated polling effect"
      contains: "visibilitychange"
    - path: "frontend/src/hooks/useTerminal.test.ts"
      provides: "test coverage proving the interval stops/resumes on visibilitychange"
      min_lines: 40
  key_links:
    - from: "frontend/src/hooks/useTerminal.ts"
      to: "document.visibilityState"
      via: "visibilitychange event listener gating setInterval"
      pattern: "addEventListener\\(.visibilitychange."
</must_haves>
---

<objective>
Gate `useTerminal`'s 15-second REST reconciliation poll (`fetchPortfolio`/`fetchWatchlist`/`fetchHistory` via `refresh()`) on `document.visibilityState` so a backgrounded browser tab stops polling entirely, and polling resumes (with an immediate refresh) the moment the tab is foregrounded again. Implements PERF-01.

Purpose: CONCERNS.md flags the current unconditional `setInterval` as wasted network/CPU work when the tab isn't visible — a real cost for a page meant to be left open on a second monitor.
Output: `useTerminal.ts` with visibility-aware polling; a new colocated `useTerminal.test.ts` proving the stop/resume behavior with fake timers.
</objective>

<execution_context>
@/Users/williamshi/Projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamshi/Projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/hooks/useTerminal.ts
@frontend/src/hooks/usePriceStream.test.ts
</context>

<interfaces>
<!-- Current polling effect in useTerminal.ts (lines 49-53), to be replaced: -->
From frontend/src/hooks/useTerminal.ts:
```typescript
useEffect(() => {
  void refresh();
  const timer = setInterval(() => void refresh(), 15_000);
  return () => clearInterval(timer);
}, [refresh]);
```

`refresh` is a `useCallback(async () => {...}, [])` already defined above this effect — do not change its signature or body, only the effect that schedules it.

<!-- Test-file conventions to mirror (usePriceStream.test.ts): plain vitest describe/it, imports named exports directly, no renderHook used there since usePriceStream exports a testable pure function. useTerminal has no such pure export for polling, so this new test file must use @testing-library/react's renderHook + vi.useFakeTimers(), following the vi.mock("@/lib/api", ...) pattern from frontend/src/components/ChatPanel.test.tsx (mocks the api module, not global fetch). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the 15s reconciliation poll on document visibility, with test coverage</name>
  <files>frontend/src/hooks/useTerminal.ts, frontend/src/hooks/useTerminal.test.ts</files>
  <read_first>frontend/src/hooks/useTerminal.ts, frontend/src/hooks/usePriceStream.test.ts, frontend/src/components/ChatPanel.test.tsx, frontend/vitest.setup.ts, frontend/src/lib/types.ts</read_first>
  <behavior>
    - Test 1 ("polls every 15s while the tab stays visible"): render the hook with `vi.useFakeTimers()` active and `document.visibilityState` left at its jsdom default ("visible"); assert `api.fetchPortfolio` is called once on mount, and called a second time after `vi.advanceTimersByTime(15_000)`.
    - Test 2 ("stops polling when the tab is backgrounded"): render the hook, let the mount-time refresh settle, then set `document.visibilityState` to `"hidden"` via `Object.defineProperty(document, "visibilityState", { value: "hidden", configurable: true })` and dispatch `new Event("visibilitychange")` on `document`; advance fake timers by 30_000ms; assert the call count on `api.fetchPortfolio` does not increase past its pre-hide count.
    - Test 3 ("resumes polling, with an immediate refresh, when the tab is foregrounded again"): starting from the hidden state in Test 2, set `document.visibilityState` back to `"visible"` and dispatch `visibilitychange` again; assert an immediate additional `api.fetchPortfolio` call happens (without advancing timers), then advance by 15_000ms and assert another call follows.
  </behavior>
  <action>
    Write frontend/src/hooks/useTerminal.test.ts first (RED): mock the `@/lib/api` module (`vi.mock("@/lib/api", () => ({ fetchPortfolio: vi.fn(), fetchWatchlist: vi.fn(), fetchHistory: vi.fn(), addTicker: vi.fn(), removeTicker: vi.fn(), executeTrade: vi.fn() }))`), have each mocked fetch* resolve to an empty/default value matching its return type from lib/types.ts (Portfolio, WatchlistItem[], Snapshot[]), use `renderHook(() => useTerminal({}))` from `@testing-library/react` inside `act()` blocks per the three behaviors above, restore real timers in an `afterEach` alongside the existing `vitest.setup.ts` global `afterEach` (which already calls `cleanup()`/`vi.restoreAllMocks()` — do not duplicate that, only add `vi.useRealTimers()`). Confirm tests fail against the current unconditional-interval implementation.
    Then modify frontend/src/hooks/useTerminal.ts's polling `useEffect` (GREEN): replace the current `setInterval`-only effect with one that defines local `start`/`stop` closures around a `timer` variable, adds a `document.addEventListener("visibilitychange", handleVisibility)` listener where `handleVisibility` calls `stop()` when `document.visibilityState === "hidden"` and `start()` otherwise, calls `start()` immediately on mount only if the tab is not already hidden, and on cleanup calls both `document.removeEventListener("visibilitychange", handleVisibility)` and `stop()`. Keep the 15_000ms interval constant and the `refresh` dependency unchanged — this task only changes when the interval runs, not what it calls.
  </action>
  <verify>
    <automated>cd frontend && npm test -- useTerminal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- useTerminal.test.ts` exits 0 with all three behaviors passing
    - `frontend/src/hooks/useTerminal.ts` contains a `"visibilitychange"` event listener registration and a matching removal in the effect's cleanup function
    - No change to `refresh`'s signature, the 15_000 interval constant, or any other exported member of `useTerminal.ts`
    - `cd frontend && npm test` (full suite) exits 0
  </acceptance_criteria>
  <done>useTerminal.ts's polling effect stops on visibilitychange->hidden and resumes with an immediate refresh on visibilitychange->visible; npm test -- useTerminal.test.ts and the full frontend suite both exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| browser tab visibility state -> polling scheduler | `document.visibilityState` is a browser-controlled, non-attacker-influenced signal read by client JS to decide whether to poll `/api/*` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03-01 | Denial of Service | `useTerminal.ts` polling effect | accept | Visibility gating only reduces request volume; it cannot be manipulated by a remote attacker to increase load, so no mitigation needed beyond this fix itself |
| T-03-02 | Tampering | `useTerminal.test.ts` — fake `document.visibilityState` override | accept | Test-only `Object.defineProperty` override is scoped to jsdom's `document` in the test process, never reaches production code paths |
</threat_model>

<verification>
- `cd frontend && npm test` — full suite (baseline 76 + new useTerminal tests) passes
- `cd frontend && npm run lint` — no new lint errors introduced by the effect rewrite
- Manual: open the app, switch to another browser tab for >15s, confirm no `/api/portfolio` network requests fire in devtools while backgrounded, then switch back and confirm a request fires immediately
</verification>

<success_criteria>
Backgrounding the tab stops the 15s poll; foregrounding resumes it with an immediate refresh; all existing and new frontend tests pass.
</success_criteria>

<output>
Create `.planning/phases/03-performance-hardening/03-01-SUMMARY.md` when done
</output>
