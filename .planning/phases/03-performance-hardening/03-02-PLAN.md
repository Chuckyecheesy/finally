---
phase: 03-performance-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/tests/api/test_app.py
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "A trade executed within a few seconds of the periodic 30s snapshot tick does not produce a near-duplicate portfolio_snapshots row"
    - "The periodic snapshot loop still records a snapshot on a tick when no recent snapshot exists"
    - "The trade-triggered snapshot in backend/app/api/portfolio.py is unaffected — it calls app.db.record_snapshot directly, unconditionally, and this plan does not touch that file"
    - "The dedup guard lives in _record_snapshot, shared by both the startup call and the periodic loop; the startup call typically records immediately since it runs against an empty or long-stale table, but if a restart happens within SNAPSHOT_DEDUP_WINDOW_SECONDS of the last snapshot, the startup snapshot is also skipped — an accepted low-risk edge case, not a bug"
  artifacts:
    - path: "backend/app/main.py"
      provides: "dedup guard in _record_snapshot skipping periodic writes within a time window of the most recent snapshot"
      contains: "list_snapshots"
    - path: "backend/tests/api/test_app.py"
      provides: "tests proving the periodic snapshot is skipped when recent, and recorded when not"
      min_lines: 130
  key_links:
    - from: "backend/app/main.py::_record_snapshot"
      to: "app.db.list_snapshots"
      via: "querying the most recent snapshot's recorded_at before deciding whether to write"
      pattern: "list_snapshots\\("
</must_haves>
---

<objective>
Prevent the 30-second periodic snapshot loop (`_snapshot_loop` in `backend/app/main.py`) from writing a near-duplicate `portfolio_snapshots` row when a trade-triggered snapshot (`backend/app/api/portfolio.py`'s `post_trade`) already landed within a short window of the current tick. Implements PERF-02.

Purpose: CONCERNS.md flags that active trading near a 30s tick boundary produces two snapshots seconds apart, adding noise to the P&L chart's data (`portfolio_snapshots` table) without adding signal.
Output: `_record_snapshot` in `backend/app/main.py` gains a time-window dedup check against the most recent existing snapshot before writing; new tests in `backend/tests/api/test_app.py` cover both the skip and the record path.
</objective>

<execution_context>
@/Users/williamshi/Projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamshi/Projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/main.py
@backend/app/db/snapshots.py
@backend/tests/api/test_app.py
@backend/tests/db/test_snapshots.py
</context>

<interfaces>
<!-- Current _record_snapshot / _snapshot_loop in backend/app/main.py (lines 37-62), to be modified: -->
From backend/app/main.py:
```python
SNAPSHOT_INTERVAL_SECONDS = 30.0

def _record_snapshot(price_cache: PriceCache, user_id: str = DEFAULT_USER_ID) -> None:
    record_snapshot(total_value=build_portfolio(price_cache, user_id).total_value, user_id=user_id)

async def _snapshot_loop(price_cache, user_id=DEFAULT_USER_ID, interval=SNAPSHOT_INTERVAL_SECONDS) -> None:
    while True:
        await asyncio.sleep(interval)
        try:
            await asyncio.to_thread(_record_snapshot, price_cache, user_id)
        except asyncio.CancelledError:
            raise
        except Exception:
            logger.exception("Failed to record portfolio snapshot")
```

`_record_snapshot` is called from three places: once at startup (line 78, unconditionally, before the loop starts), once per periodic tick from `_snapshot_loop`, and it is NOT what `backend/app/api/portfolio.py`'s `post_trade` calls — that endpoint calls `app.db.record_snapshot` (the DB-layer function) directly and must keep doing so unconditionally; do not modify `backend/app/api/portfolio.py` in this plan.

From backend/app/db/snapshots.py (already supports what this task needs, no changes required to this file):
```python
def list_snapshots(user_id=DEFAULT_USER_ID, since=None, limit=None, conn=None) -> list[PortfolioSnapshot]:
    """Snapshots ordered oldest to newest... `limit` keeps the most *recent* N while preserving oldest-first ordering."""
```
`PortfolioSnapshot.recorded_at` is an ISO-8601 string produced by `app.db.utc_now_iso()` (`datetime.now(UTC).isoformat()`), parseable with `datetime.fromisoformat`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedup periodic snapshots against a recent trade-triggered or prior snapshot</name>
  <files>backend/app/main.py, backend/tests/api/test_app.py</files>
  <read_first>backend/app/main.py, backend/app/db/snapshots.py, backend/app/db/models.py, backend/tests/api/test_app.py, backend/tests/api/conftest.py, backend/tests/db/test_snapshots.py</read_first>
  <behavior>
    - Test 1 ("periodic snapshot is skipped when a recent snapshot already exists"): using the `temp_db` and `price_cache` fixtures from `backend/tests/api/conftest.py`, call `app.db.record_snapshot(total_value=100.0)` directly (simulating a just-happened trade-triggered snapshot with `recorded_at` at "now"), then call `main._record_snapshot(price_cache)` (simulating the next periodic tick); assert `len(app.db.list_snapshots())` is still 1 (no new row written).
    - Test 2 ("periodic snapshot is recorded when the most recent snapshot is older than the dedup window"): call `app.db.record_snapshot(total_value=100.0, recorded_at="2020-01-01T00:00:00+00:00")` (far in the past), then call `main._record_snapshot(price_cache)`; assert `len(app.db.list_snapshots())` is now 2.
    - Test 3 ("no prior snapshot at all still records"): with an empty `portfolio_snapshots` table (fresh `temp_db`), call `main._record_snapshot(price_cache)` directly; assert `len(app.db.list_snapshots())` is 1 — this guards against the dedup check crashing or wrongly skipping on `list_snapshots()` returning `[]`.
  </behavior>
  <action>
    Add these three tests to `backend/tests/api/test_app.py` first (RED) — they can live standalone using the `temp_db`/`price_cache` fixtures (no need for the `started_app`/`TestClient` lifespan machinery used by the existing startup/shutdown tests in this file), calling `main._record_snapshot` and `app.db.record_snapshot`/`list_snapshots` directly. Run `uv run --extra dev pytest tests/api/test_app.py -k dedup -x` (or the equivalent test names you choose) and confirm they fail against the current unconditional implementation.
    Then modify `backend/app/main.py` (GREEN): add a module-level `SNAPSHOT_DEDUP_WINDOW_SECONDS = 5.0` constant near `SNAPSHOT_INTERVAL_SECONDS`; import `list_snapshots` alongside the existing `from app.db import DEFAULT_USER_ID, init_db, list_watchlist, record_snapshot` and add `from datetime import UTC, datetime` to the module's imports. Rewrite `_record_snapshot` to first call `list_snapshots(user_id, limit=1)`; if that returns a non-empty list, parse the single row's `recorded_at` with `datetime.fromisoformat` and compare `(datetime.now(UTC) - parsed).total_seconds()` against `SNAPSHOT_DEDUP_WINDOW_SECONDS` — if the elapsed time is less than the window, return without calling `record_snapshot`; otherwise (or if the list was empty) proceed to call `record_snapshot` exactly as it does today. Leave `_snapshot_loop`, the startup call at line 78, and `SNAPSHOT_INTERVAL_SECONDS` unchanged — the dedup lives entirely inside `_record_snapshot` so both its callers (startup and the periodic loop) share the same guard, which is safe since startup always runs against an empty or long-stale table.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/api/test_app.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev pytest tests/api/test_app.py -v` exits 0, including the three new dedup tests
    - `backend/app/main.py` defines `SNAPSHOT_DEDUP_WINDOW_SECONDS` and `_record_snapshot` calls `list_snapshots` before deciding whether to write
    - `backend/app/api/portfolio.py` is untouched by this plan (its `post_trade` snapshot call remains unconditional)
    - `cd backend && uv run --extra dev pytest` (full backend suite) exits 0
    - `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
  </acceptance_criteria>
  <done>_record_snapshot skips writing when the most recent snapshot is within SNAPSHOT_DEDUP_WINDOW_SECONDS, still writes when none exists or the most recent is older than the window, and backend/app/api/portfolio.py is untouched; full backend suite exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| background asyncio task -> SQLite writes | `_snapshot_loop` runs unattended in-process; its only external input is wall-clock time and the price cache, neither attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03-03 | Tampering | `_record_snapshot` dedup window comparison | accept | `recorded_at` values are server-generated (`utc_now_iso()`), never client-supplied, so there is no injection surface for the dedup comparison |
| T-03-04 | Denial of Service | `SNAPSHOT_DEDUP_WINDOW_SECONDS` constant | accept | A fixed 5s window bounded well under the 30s interval; cannot be tuned by request input, so no DoS surface is introduced |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest` — full suite (baseline 280 + 3 new tests) passes
- `cd backend && uv run --extra dev ruff check app/ tests/` — no new lint errors
- Manual: with `LLM_MOCK=true`, run the app, execute two trades a few seconds apart around a 30s boundary, and confirm `db/finally.db`'s `portfolio_snapshots` table doesn't show two rows within `SNAPSHOT_DEDUP_WINDOW_SECONDS` of each other
</verification>

<success_criteria>
A trade executed within a few seconds of a periodic 30s tick does not produce a near-duplicate snapshot row; the periodic loop still records normally when no recent snapshot exists; all existing and new backend tests pass.
</success_criteria>

<output>
Create `.planning/phases/03-performance-hardening/03-02-SUMMARY.md` when done
</output>
